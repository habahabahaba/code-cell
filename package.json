{
  "name": "code-cell",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "all:update": "pnpm -r update -i -L",
    "to_api": "pnpm --filter=@code-cell/local-api",
    "to_cli": "pnpm --filter=@code-cell/cli",
    "to_client": "pnpm --filter=@code-cell/local-client",
    "clean": "find ./ -name node_modules -type d -exec rm -rf {} +",
    "dev": "pnpm -r --parallel --stream run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "eslint": "^9.6.0",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  }
}
